=== v10 배포 가이드 ===

파일별 GitHub 경로:
  automation.html      → templates/automation.html
  app.py               → (루트)
  requirements.txt     → (루트)
  mission_template.xlsx → static/templates/mission_template.xlsx

주요 변경사항 (v10):
  1. v15 컬럼 구조 반영: A~O (15열), H=포인트(10)수식, I=검색어(PID앞3)
     J=미션내용, K=정답(PID앞5), L=힌트URL, M=업체명, N=일유입, O=글자수수식
  2. 병합 열: A,C,D,E,F,G,H(포인트),N,O → 5행씩 (9개 열)
  3. 수식 보존: E,F,G,H,O (기존 수식 건드리지 않음)
  4. 데이터 시작행: 3 (v15 파일 기준)
  5. 힌트URL(L열) 자동 생성: acq(길이별), acr(랜덤1-9)
  6. 키워드 쉼표 구분 입력 → 5행 순환 분배
  7. 업체명(M열): Naver Shopping API mallName 우선, 실패시 크롤링
  8. /api/check-rank 순위 확인 API 추가 (top15 초과시 경고)
  9. 파일 업로드 UI 제거 → 📁 템플릿에 채우기 버튼 하나로 통합
  10. 미사용 버튼 제거, UI 컴팩트화

환경변수 (Render):
  NAVER_CLIENT_ID     = 네이버 API 클라이언트 ID
  NAVER_CLIENT_SECRET = 네이버 API 시크릿
